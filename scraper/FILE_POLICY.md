# 📁 Scraper 폴더 구조 및 파일 정책

## 폴더 구조

```
scraper/
├── input/              # 입력 이미지
│   ├── source/         # 수동 입력 원본
│   └── scraped/        # 스크래핑된 원본
├── output/             # 이미지 출력
│   └── temp_candidates/  # 임시 후보
├── reports/            # 스크래핑 결과
│   ├── daily/          # 일별 결과
│   └── archive/        # 아카이브
├── generators/         # 이미지 생성
├── crawlers/           # 크롤러
├── utils/              # 유틸리티
│   └── paths.py        # 경로 관리
└── FILE_POLICY.md      # 이 문서
```

---

## 파일 생성 정책

### 입력 → `input/`
| 유형 | 경로 | 예시 |
|------|------|------|
| 수동 입력 | `input/source/` | 직접 넣은 광고 이미지 |
| 스크래핑 원본 | `input/scraped/` | 크롤러가 다운로드한 원본 |

### 출력 → `output/`
| 유형 | 경로 | 예시 |
|------|------|------|
| 최종 이미지 | `output/` | `ad_final.png` |
| 임시 후보 | `output/temp_candidates/` | 자동 삭제 |

### 결과물 → `reports/`
| 유형 | 경로 | 예시 |
|------|------|------|
| 일별 데이터 | `reports/daily/` | `2024-12-16_results.xlsx` |
| 아카이브 | `reports/archive/` | 월별 백업 |

---

## 사용법
```python
from utils.paths import PATHS

# 입력 원본 저장
source_img = PATHS["input_source"] / "my_ad.png"

# 출력 이미지
output = PATHS["output"] / "ad_final.png"

# 결과 엑셀
report = PATHS["reports_daily"] / "results.xlsx"
```
